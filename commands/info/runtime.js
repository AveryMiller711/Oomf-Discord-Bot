const Discord = require("discord.js");

module.exports = {
    name: 'runtime',
    category: 'info',
    description: 'Total runtime of the bot.',
    execute(message, args){
        return;
    },
};